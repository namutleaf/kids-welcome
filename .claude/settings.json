{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "(npm run dev > /dev/null 2>&1 &) && echo '{\"systemMessage\": \"Dev server running at http://localhost:3000. Check the artifact for the current state of the app.\"}' || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
